# Chronos-Spatial training pipeline (v1: fully AI-generated detection) — Python 3.10+
#
# torch/torchvision: install the CUDA build if you have an NVIDIA GPU, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.2
torchvision>=0.17

timm==1.0.11
albumentations==1.4.18
opencv-python==4.10.0.84
scikit-learn==1.5.2
pandas==2.2.3
PyYAML==6.0.2
tqdm==4.66.5
matplotlib==3.9.2
numpy>=1.26

# Optional, faster video decoding. Windows wheels are often unavailable —
# the pipeline falls back to OpenCV automatically if this is missing.
# decord==0.6.0

# --- local inference server (src/server.py) — connects the frontend to a
# real trained checkpoint instead of mock data. Not needed for training.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
